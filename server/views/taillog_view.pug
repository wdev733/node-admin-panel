section.content
	// Title
	.page-header
		h1 Output the last lines of the pihole.log file (live)
	.checkbox
		label
			input#chk1(type='checkbox', name='active', checked='')
			|  Automatic scrolling on update
	pre#output(style='width: 100%; height: 100%;').
	.checkbox
		label
			input#chk2(type='checkbox', name='active', checked='')
			|  Automatic scrolling on update
